{{ "{% extends 'VaszevCrudBundle::layout.html.twig' %}" }}

{{ "{% block title %}" }}
{{ "{{ parent() }} -  {{ 'views.edit.edit'|trans({'%entity%': '" }}{{ entity }}{{ "'}, 'VaszevCrudBundle') }}" }}
{{ "{% endblock %}" }}

{{     "{% block menu %}" }}
{{      "{% block stylesheets %}" }}
{{        "{{ parent() }}" }}
{{        "{% block stylesheets_sub %}{% endblock %}" }}
{{      "{% endblock %}" }}
{{      "{% include 'vaszevCrudMenu.html.twig' %}" }}
{{    "{% endblock %}" }}

{{ "{% block page %}" }}
<h1>{{ "{{ 'views.edit.edit'|trans({'%entity%': '" }}{{ entity }}{{ "'}, 'VaszevCrudBundle') }}" }}</h1>

{#<form class="well" action="{{ "{{ path('"~ route_name_prefix ~"_update', { 'id': entity.id }) }}" }}" method="post" {{ "{{ form_enctype(edit_form) }}" }}>#}
    {{ "{{ form_start(edit_form,{'attr':{'class':'well'}}) }}" }}
    {{ "{{ form_widget(edit_form) }}" }}
    <p>
        <button type="submit" class="btn btn-success">{{ "{{ 'views.edit.editbutton'|trans({}, 'VaszevCrudBundle') }}" }}</button>
    </p>
    {{ "{{ form_end(edit_form) }}" }}
{#</form>#}

{% set hide_edit, hide_delete = true, false %}
{% include 'crud/views/others/record_actions.html.twig.twig' %}

{{ "{% include 'vaszevCrudFooter.html.twig' %}" }}

{{ "{% endblock %}" }}